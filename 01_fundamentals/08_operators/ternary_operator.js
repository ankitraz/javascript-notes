// if a customer has more than 100 points they are 'gold' custormer otherwidse they are 'silver' customer

let customerPoints = 110;

let customerType = customerPoints > 100 ? 'gold' : 'silver';
console.log(customerType);